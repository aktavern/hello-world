def name_age():
    print('Hello!')
    # Checks that the input contains letters. 1 bug: if a space is added with a digit it will not be caught by this. I need to find a better way to check that myName truly does not contain ANY numbers.
    while True:
        myName = input('What is your name? ')
        if (myName.isdigit() == True):
            print('Please only enter letters.')
            continue
        else:
             break
   # Converts myName to a string and calculates the length of the name, Excluding any spaces.
   print('Thank you, ' + myName + '. The length of your name is: ' + str((len(myName) - myName.count(' '))))
   # Checks that the input contains only numbers. 
    while True:
        myAge = input('What is your age? ')
        if (myAge.isalpha() == True):
            print('Please only enter numbers.')
            continue
        else:
            break
    # Do some math on the age provided. 
    import datetime
    now = datetime.datetime.now()
    print('Thank you, ' + myName + '. You were born in the year: ' + str((now.year - 1) - (int(myAge))))
    print('Half of your age is ' + str(int(myAge) / 2) + '.')
    print('Good bye!')
